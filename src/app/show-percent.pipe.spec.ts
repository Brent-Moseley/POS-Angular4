import { ShowPercentPipe } from './show-percent.pipe';

describe('ShowPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
